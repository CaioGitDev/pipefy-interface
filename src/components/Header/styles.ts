import styled from "styled-components";

export const Container = styled.div`
  height: 80px;
  padding: 0 30px;
  background: var(--primary);
  color: var(--white);
  display: flex;
  align-items: center;
`;